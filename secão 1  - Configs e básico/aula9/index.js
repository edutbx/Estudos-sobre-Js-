const nome = 'Eduardo'; // stings
const nome1 = "Eduardo";// stings
const nome2 = `Eduardo`;// stings
const num1 = 10; //nummber
const num2 = 10.54; //nummber
let nomeAluno; // undefined -> não aponta para nenhum local na memória
let sobrenomeAluno = null; // null ->não aponta para nenhum local na memória (desconfigura o valor da variável)
const aprovado = false; //boolean = true, false (lógico) 


const teste = 2;
const teste1 = 20;
console.log(teste ** teste1);